root = "."
tmp_dir = "tmp"

[build]
    cmd = "go build -o tmp/main ./cmd/consumer"
    bin = "./tmp/main"
    full_bin = ""
    include_ext = ["go", "tpl", "tmpl", "html"]
    exclude_dir = ["assets", "tmp", "vendor"]
